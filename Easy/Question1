#include <stdio.h>
#include <conio.h>

int main()
{	
	int x,i,a;	
	printf("enter the no :- ");
	scanf("%d",x);	
	if(0<x<13)
	{	
	for(i=1;i<11;i++)
		{
		a=x*i;
		printf("%d x %d = %d",x,i,a);
		}
		return 0;
	}	
	printf("The no is no between 1 and 12");
	return 0;
}
