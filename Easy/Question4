Write a function that tests whether a string is a palindrome.

public static int[] odd(string[] a)
{
  int n= a.length;
  int m= n/2;
  for(int i=0;i<m;i++)
  {
    if(a[i]!=a[n-i-1])
    {
      return false;
    }
  }
  return true;
  
}
